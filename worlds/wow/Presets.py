from typing import Any, Dict

from .Options import *

WOW_option_presets: Dict[str, Dict[str, Any]] = {
    # Standard playthrough
    "Placeholder": {
        "Honor In Pool": 10,
        "Honor To Win": 20
    }
}
